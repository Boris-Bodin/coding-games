import { Card } from './card';

export class GreenCard extends Card {

    public value(): number {
        if (this.defense = 3) {
           // return 300;
        }
        return super.value();
    }
}
